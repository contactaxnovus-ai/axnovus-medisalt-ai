PORT=8000

# Optional. When set, /api/extract uses an LLM to clean OCR/prescription text.
# Salt matching still stays deterministic in src/medicine-agent.js.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
